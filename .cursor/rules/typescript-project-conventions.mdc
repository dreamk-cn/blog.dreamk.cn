---
description: TypeScript 通用编码约定
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# TypeScript 项目约定

- 使用 `@/` 引用 `src` 下模块，避免多层相对路径。
- 优先导出具名函数与具名常量，不要为普通业务代码引入 class 风格。
- 在函数边界写清参数对象和返回值语义；局部变量能依赖推断时不要重复标注。
- 对 `null` / `undefined` 保持显式处理，不要依赖隐式真假判断掩盖“未找到”和“空值”。
- 公共状态、枚举和值对象优先复用 Prisma / Zod / `src/types` 已有定义，不重复造类型。
- 工具函数保持单一职责，私有 helper 放在就近文件内部，跨模块复用再下沉到 `src/lib`。
- 注释只解释约束、业务意图或陷阱，不写“变量赋值”这类低信息注释。
- 新增代码默认遵循现有风格：单引号或分号以所在文件为准，不为了个人偏好做大面积格式漂移。
- 例：查询不到记录时优先返回 `null`，由 route 或 page 决定提示文案，而不是在 service 中直接抛泛化错误。
